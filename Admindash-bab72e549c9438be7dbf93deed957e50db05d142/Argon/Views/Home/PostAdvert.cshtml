@{
	ViewData["ShowNavbar"] = false;  // Navbar'ı engelle
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />


<div class="container mt-5">
	<div class="card shadow border-0">
		<div class="card-header bg-gradient-primary text-white">
			<h3 class="mb-0">Logo əlavə et</h3>
		</div>
		<div class="card-body">
			<div class="container mt-5">
				<form id="categoryForm" method="post" action="" enctype="multipart/form-data">
					<div class="mb-3">
						<label for="videoTitle" class="form-label">Reklam adı</label>
                        <input type="text" class="form-control" id="videoTitle" name="advertName" required placeholder="Reklam adı">
					</div>
                    <div class="mb-3">
                        <label for="videoTitle" class="form-label">Reklam adı</label>
                        <input type="text" class="form-control" id="videoTitle" name="advertContext" required placeholder="Reklam məlumatı">
                    </div>
					<div class="mb-3">
						<label for="videoUrl" class="form-label">Vi</label>
                        <input type="file" class="form-control" id="videoUrl" name="advertCoverFile" required>
					</div>
                    <div class="mb-3">
                        <label for="videoUrl" class="form-label">Vi</label>
                        <input type="file" class="form-control" id="videoUrl" name="advertVideoUrl" required>
                    </div>
					<button type="submit" class="btn btn-primary w-100">Əlavə et</button>
				</form>
			</div>
		</div>
	</div>
</div>


<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script> <!-- SweetAlert2 Kütüphanesini ekliyoruz -->

<script>
        document.getElementById("categoryForm").addEventListener("submit", async (event) => {
        event.preventDefault();

        const formData = new FormData(event.target);

        try {
            const response = await fetch("https://localhost:44314/api/advert/upload", {
                method: "POST",
                body: formData,
            });

            if (response.ok) {
                const result = await response.json();
                Swal.fire({
                    icon: 'success',
                    title: 'Uğurlu əməliyyat',
                    text: "Reklam uğurla əlavə edildi",
                }).then(() => {
                    location.reload();
                });
            } else {
                const errorText = await response.text();
                Swal.fire({
                    icon: 'error',
                    title: 'Xəta',
                    text: errorText,
                });
            }
        } catch (error) {
            console.error("İstek başarısız oldu:", error);
            Swal.fire({
                icon: 'error',
                title: 'Xəta',
                text: 'Reklam əlavə edilə bilmədi',
            });
        }
    });

</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>